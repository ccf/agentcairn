[project]
name = "agentcairn"
dynamic = ["version"]
description = "Local-first agent memory: an Obsidian markdown vault as source of truth, with a rebuildable DuckDB index."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [{ name = "Charles C. Figueiredo", email = "ccf@ccf.io" }]
keywords = ["agent-memory", "mcp", "obsidian", "duckdb", "rag", "local-first", "claude", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Operating System :: OS Independent",
]
dependencies = [
    "typer>=0.12",
    "python-frontmatter>=1.1",
    "markdown-it-py>=3.0",
    "fastembed>=0.4",
    "duckdb>=1.1",
    # Upper bound is load-bearing: mcp 2.0.0 removed `mcp.server.fastmcp`
    # (renamed to `mcp.server.mcpserver`), which cairn.mcp.server imports. Without
    # the bound, uvx/uv resolve 2.0.0 and the MCP server dies on import (#152).
    # Widen only together with migrating cairn/mcp/server.py to the 2.x API.
    "mcp>=1.26.0,<2.0.0",
    "tomlkit>=0.15.0",
]

[project.scripts]
cairn = "cairn.cli:app"
agentcairn = "cairn.mcp.server:main"

[project.urls]
Homepage = "https://agentcairn.dev"
Repository = "https://github.com/ccf/agentcairn"
Documentation = "https://github.com/ccf/agentcairn#readme"

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.6", "pre-commit>=3.8"]
bench = ["anthropic>=0.40", "huggingface-hub>=0.25"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/cairn/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/cairn"]

[tool.pytest.ini_options]
pythonpath = ["src", ".", "benchmarks"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]
# ruff 0.16 formats Python code blocks inside Markdown. docs/plans and docs/specs
# are dated historical records whose snippets are illustrative, not compiled — keep
# them byte-stable instead of rewriting history on every formatter release.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: typer.Argument/typer.Option in default positions is the intended Typer API pattern.
ignore = ["B008"]
